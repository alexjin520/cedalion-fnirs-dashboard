# fNIRS Dashboard 功能地图

本文档说明当前仪表盘已经支持的功能、用户在哪里可以看到或使用它，以及对应的服务端接口。它是面向使用者和维护者的功能清单，不替代 `README.md` 中的安装、部署和科学参数说明。

## 使用入口

启动服务后访问 `http://<host>:<port>/`。所有页面共用顶部导航，可在“总览”“信号浏览”“任务响应”和“质量与记录”之间切换；右上角显示服务连接状态，并提供刷新/重新分析入口。总览页选定记录后，导航、图表请求、质量表跳转和 CSV 导出均会保留同一个 `recording` 选择。

| 功能 | 显示或操作位置 | 说明 |
| --- | --- | --- |
| 记录总览 | `/` | 显示当前 SNIRF 文件名、UTF-8 `SubjectID`、Cedalion 版本、文件大小、波长、DPF、采样率、采样点数、记录时长、分析通道数及事件数。若 `SubjectID` 含非 ASCII 字符，分析使用仅在读取期间存在的 ASCII 兼容副本，原文件不修改。 |
| 多记录选择 | `/` 顶部“分析记录” | 列出 `FNIRS_DATA_DIR` 内的 `.snirf` 文件；选择后以 `?recording=<相对路径>` 打开该记录，目录列表本身不触发 Cedalion 分析。文件名含 BIDS 风格 `sub-` / `ses-` 时，列表接口会提供相应筛选字段。 |
| 分析清单下载 | `/` 顶部“下载分析清单” | 下载当前记录的 JSON 清单，包含输入 SHA-256、稳定分析 ID、完成时间、软件版本、参数、校验、排除原因、任务和 GLM 状态。 |
| PDF 研究报告 | `/` 顶部“生成 PDF 报告” | 生成当前单条记录的 PDF，包含分析概况、质量事实、代表性任务响应、GLM 状态、分析参数和研究者备注位置；任务或 GLM 不可用时报告记录明确原因。 |
| 数据就绪度 | `/` 的“这份记录可以继续分析吗？” | 根据通道通过率和任务分析可用性显示“可以继续”或“建议复核”，并汇总需关注通道、可用条件、运动校正方式和 GVTD 异常候选。 |
| 处理流程 | `/quality` 的“分析流程”标签 | 展示 SNIRF 读取、光密度转换、TDDR、HbO/HbR 滤波和任务响应的当前处理顺序。 |

## 连续信号浏览

页面地址：`/signals`，兼容地址：`/signals.html`。

| 功能 | 显示或操作位置 | 说明 |
| --- | --- | --- |
| 信号类型切换 | 图表上方“原始 / OD / HbO/HbR / 滤波后”分段控制 | 按通道查看原始光强、光密度、血氧浓度或滤波后浓度；滤波后可比较 TDDR + CBSI。 |
| 信号校正比较 | 图表上方“信号校正” | 仅在“滤波后”信号下可选未校正、TDDR、TDDR + CBSI 或实验性 Wavelet；Wavelet 不与 TDDR 叠加。 |
| 波长或血红蛋白成分选择 | 图表上方“波长 / 成分” | 原始和 OD 选择波长；浓度信号选择 HbO 或 HbR。 |
| 通道选择与质量检查 | 右侧“通道检查器” | 选择通道后显示光源/探测器、平均/最低 SNR、平均 SCI、PSP 中位数及合格窗口比例、质量判定和当前信号统计。 |
| 图表交互 | 中央时间序列图 | 曲线标注任务区间和 GVTD 异常候选；拖动可放大时间范围，双击或“重置”可恢复完整范围，悬停可查看数值。图表数据会降采样，统计值基于完整数据。 |
| 指定通道直达 | `/signals?channel=<index>` | 通过通道索引直接打开对应通道，也由质量表的“查看信号”链接使用。 |

## 任务响应

页面地址：`/task`，兼容地址：`/task.html`。

| 功能 | 显示或操作位置 | 说明 |
| --- | --- | --- |
| 条件与通道选择 | 图表上方的“任务条件”和“合格通道” | 条件来自记录中的任务区间；通道列表仅包含通过 SNR 和 SCI 门限、且未被任务短间距排除的通道。 |
| 任务锁定响应 | 中央图表 | 使用 TDDR、基线校正和条件分段，显示 HbO/HbR 响应及刺激区间。有两个及以上区间时显示条件平均和 SEM；单区间不提供 SEM。 |
| 响应指标 | 图表右侧 | 显示纳入区间数、HbO 峰值和潜伏期、HbR 谷值和潜伏期，以及质量筛选后的通道数。峰值/谷值在刺激后 3–15 秒响应窗内计算。 |
| 导出 CSV | 顶部“导出 CSV” | 下载当前条件和当前通道的时间、HbO/HbR 响应及可用 SEM；每行还带有 `analysis_id`、输入 SHA-256 和记录 ID。 |
| 保存 PNG | 图表右上“保存图像 PNG” | 保存当前任务响应图。 |
| GLM 条件效应 | 任务平均图下方“GLM 统计推断”左表 | 用 Gamma HRF、余弦漂移和质量合格通道拟合，显示所选条件及通道的 HbO/HbR beta、95% CI、t、p、FDR q 和 R²。短间距回归启用时，选择器中的“仅平均”通道不作为 GLM 目标；原有任务平均不受影响。 |
| GLM 条件对比 | “GLM 统计推断”中的“条件对比”选择器和右表 | 选择两个条件的有符号差，例如“左手敲击 - 右手敲击”，查看每个血红蛋白成分的效应差、95% CI、t、p、FDR q 和 R²。 |
| 导出 GLM CSV | “GLM 统计推断”右上“导出 GLM CSV” | 下载所有建模通道的条件效应与条件对比，而不只限当前通道；每行带有 `analysis_id`、输入 SHA-256 和记录 ID。 |
| 指定通道直达 | `/task?channel=<index>` | 通过通道索引预选合格通道。 |

## 质量与记录

页面地址：`/quality`，兼容地址：`/quality.html`。

| 功能 | 显示或操作位置 | 说明 |
| --- | --- | --- |
| 质量摘要 | 页面顶部 | 显示通过/需关注通道数、SNR 和 SCI 门限、SCI 窗口，以及 GVTD 异常采样和候选区段数。 |
| 通道质量表与人工排除 | “记录详情”中的“通道质量” | 显示每个通道的光源、探测器、平均/最低 SNR、平均 SCI、PSP 中位数、PSP 合格窗口比例和通过状态。表中的“人工排除”复选框会将当前记录的坏通道决定持久化，并从任务平均和 GLM 中移除；支持搜索及“只看需关注”筛选。点击行或“查看信号”可跳转到该通道的信号浏览页。 |
| 事件标记 | “记录详情”中的“事件标记” | 按标签列出记录到的刺激事件数量。 |
| 数据元信息 | “记录详情”中的“数据元信息” | 显示文件、受试者显示名、采样、通道/测量、原始与分析通道、光强预处理、事件/任务区间、波长、Cedalion 版本、关键参数、输入校验、探头距离以及当前辅助/全局回归状态。 |
| 分析流程 | “记录详情”中的“分析流程” | 显示质量诊断、运动校正、滤波和任务响应的处理说明。 |
| 分析设置 | “记录详情”中的“分析设置” | 可修改 DPF、滤波、SNR/SCI 门限、任务时间窗、短距离/GVTD 策略及 GLM 常用参数。应用后立即重新分析并保存到服务器；可恢复为服务器启动时的环境变量配置。 |

## 数据处理与质量判定

| 功能 | 用户可见位置 | 当前行为 |
| --- | --- | --- |
| 输入校验 | `/quality` 的“数据元信息”；`/api/analysis-metadata`；`/api/probe` | 在处理前检查波长、时间轴、刺激列、几何坐标和通道距离，并返回警告。UTF-8 `SubjectID` 会从源文件读取；非 ASCII 值仅在临时分析副本中替换为 ASCII 伪名，清单记录兼容状态。`/api/probe` 还列出每通道距离及长/短间距分类。 |
| 无效光强处理 | `/` 总览；`/quality` 的“数据元信息” | 只纳入每个波长中正值比例达到要求的通道；入选通道的孤立非正采样以线性插值处理。 |
| 通道质量 | `/`、`/signals`、`/quality` | 使用 SNR、SCI、PSP 和 GVTD。通道通过条件为最低 SNR、平均 SCI 和 PSP 合格窗口比例均达到门限，且未被人工标记为坏通道。 |
| 运动候选与试次排除 | `/`、`/signals`、`/quality`、`/task` 的任务元数据 | GVTD 默认只标记异常候选采样/区段。`FNIRS_GVTD_MODE=exclude_epochs` 时，任务平均会剔除包含异常采样的完整试次；GLM 会在完整规则设计矩阵建立后同步剔除 GVTD 异常采样，并在清单中记录删点统计。任务分析默认采用 TDDR。 |
| 短距离通道 | `/api/probe`、`/api/analysis-metadata`、`/api/recording` 和 `/task` 的 GLM 元数据 | 可按距离报告，或在 `exclude` 模式下只从任务平均中排除。GLM 的 `auto` 模式在有合格的长、短通道时，会为每个建模长通道加入最近短通道回归量；该回归不改变连续信号、质量指标或任务平均。 |
| CBSI 血氧校正 | `/signals` 的“信号校正”；`/quality` 的“分析设置”和分析清单 | 始终提供 TDDR + CBSI 连续曲线比较。`FNIRS_CBSI_MODE=on` 时，按通道以完整时间序列的 HbO/HbR 标准差比执行 Cui CBSI，并用于任务平均和 GLM；默认 `off`。不可安全估计比例的通道保留原信号并写入清单。 |
| 辅助信号与回归 | `/quality` 的“数据元信息”；`/task` 的“GLM 统计推断”；`/api/auxiliary` | `FNIRS_GLM_NUISANCE_MODE` 默认 `off`。`auxiliary` 只使用 `FNIRS_GLM_AUXILIARY_SIGNALS` 明确列出的流，检查秒级时间单位、严格递增、完整覆盖、插值缺口与方差后，线性重采样到 GLM 时间轴并 z-score；非零 `time_offset` 不会被静默应用。`global` 显式将 Cedalion 的建模通道全局平均加入 GLM。`auxiliary_global` 同时请求两者。 |
| 可复现性与审计 | `/` 的“下载分析清单”；`/api/analysis-metadata` | 每次成功分析保存输入 SHA-256、稳定分析 ID、分析完成时间、Python/Cedalion/运行时依赖版本、完整参数、非正光强排除、自动和人工质量排除原因、GVTD 策略、短距离状态、辅助流对齐/拒绝原因、全局回归状态、任务与 GLM 设置。人工决定默认存于 `FNIRS_DATA_DIR/.fnirs-dashboard-qc.json`，可由 `FNIRS_QC_DECISIONS_FILE` 指定路径。进程内缓存按路径、文件大小和修改时间及人工决定独立保存最多 3 份分析结果。 |

## API

所有 `/api/*` 响应为 JSON（导出接口除外），且不缓存。除 `/api/recordings` 外，记录相关接口可加 `recording=<FNIRS_DATA_DIR 内的相对 .snirf 路径>`；省略时使用默认记录。目录外路径、非 SNIRF、空值或重复 `recording` 参数返回 `400`，缺失文件返回 `404`。

| 接口 | 用途 | 当前页面使用位置 |
| --- | --- | --- |
| `GET /api/health?recording=` | 服务与所选/默认数据文件状态 | 可用于部署健康检查；不触发完整 Cedalion 分析。 |
| `GET /api/recordings?subject=&session=` | 轻量列出数据目录内的 SNIRF、默认记录、大小、修改时间及可从 BIDS 路径识别的受试者/会话 | `/` 的“分析记录”选择器。 |
| `GET /api/recording` | 记录摘要、UTF-8 `SubjectID` 显示名、通道、信号选项、事件、区间、运动、质量和任务摘要 | 四个页面的主数据来源。 |
| `GET /api/analysis-metadata` | 完整分析清单：输入哈希、分析 ID、时间、UTF-8 `SubjectID` 显示名、临时兼容副本状态、软件版本、参数、校验、排除原因、短距离/回归、任务和 GLM 摘要 | 供 API 使用者读取完整分析元数据。 |
| `GET /api/analysis-metadata-export` | 以附件下载当前记录的完整分析清单 JSON | `/` 的“下载分析清单”。 |
| `GET /api/report-pdf` | 生成当前记录的单条分析 PDF 报告 | `/` 的“生成 PDF 报告”。 |
| `GET /api/probe` | 探头几何校验、距离和长/短间距分类 | 供 API 使用者检查探头数据。 |
| `GET /api/auxiliary` | 辅助流清单、当前选择、时间对齐审计、实际使用的辅助/全局回归量与拒绝原因 | 供 API 使用者核对生理回归可用性。 |
| `GET /api/quality` | 逐通道质量与 GVTD 摘要 | `/quality`、`/signals` 和 `/task`。 |
| `POST /api/qc-decisions` | 更新当前记录的 `bad_channel_labels` 字符串数组；仅接受该记录存在的通道 | `/quality` 的“人工排除”复选框。 |
| `GET /api/signal?kind=&channel=&component=&max_points=` | 指定通道的连续信号 | `/signals`。 |
| `GET /api/task-response?condition=&channel=&max_points=` | 指定条件和通道的任务响应 | `/task`。 |
| `GET /api/task-export?condition=&channel=` | 当前任务响应的 CSV 文件 | `/task` 的“导出 CSV”。 |
| `GET /api/task-glm?condition=&channel=&contrast=` | 指定条件、通道和对比的 GLM beta/统计量 | `/task` 的“GLM 统计推断”。 |
| `GET /api/task-glm-export` | 所有 GLM 条件效应和条件对比的 CSV 文件 | `/task` 的“导出 GLM CSV”。 |

## 维护规则

每次变更完成前，都要检查本文档是否仍与实际实现一致。新增、删除或修改任何用户可见功能、页面位置、导航、交互、接口、导出内容或分析结果展示时，必须在同一变更中更新本文档。纯内部重构且不改变上述行为时无需修改，但不得借此遗漏实际行为变化。
