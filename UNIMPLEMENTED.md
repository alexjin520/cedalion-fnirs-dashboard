# Cedalion fNIRS Dashboard 路线图

本文档只记录尚未完成或仍需正式化的工作。`[进行中]` 表示当前批次正在实现，`[ ]` 表示后续工作；完成后必须补充测试和部署记录，不能仅凭页面出现控件就标记为完成。

## P0：科学正确性护栏

- [x] 固定采样率处理只接受 1% 容差内的规则时间轴；关闭重采样时，不规则输入会在滤波、分段或 GLM 前拒绝。
- [x] 要求严格正的刺激前基线窗口，逐事件排除无法覆盖完整基线或后窗的边界事件，并在分析清单中记录排除数量和原因。
- [x] 保留 GLM 的计算结果，但区分“计算可用”和“具备重复试次的最低推断就绪度”；单试次条件只能标记为探索性。
- [x] 显式重采样流程：支持 `off` / `auto` / `force`、目标采样率、最大插值缺口和端点保持；下采样使用 Cedalion 八阶低通抗混叠，重建整数 `samples` 坐标，不改变事件秒语义，并在摘要/清单记录完整审计。
- [x] 为主要失败路径补齐真实 SNIRF、边界事件、缺列事件和非有限值回归测试；缺少显式真实样例目录时测试会跳过，指定目录后缺样例会失败。

## P0：性能与任务执行

- [x] Wavelet 实验信号改为按需计算并在分析对象内线程安全缓存；缓存失效时不再默认执行该分支。
- [x] 单条记录 PDF 已在 `/api/report-pdf` 请求时生成，不进入分析缓存主路径。
- [x] GLM 基础上下文与拟合结果分离并按需缓存；首页、质量、连续信号和任务响应接口返回 `pending`/描述性结果，任务 GLM、报告和清单接口首次请求时拟合并复用。
- [ ] 将 GLM 拟合放入可取消的异步任务队列；当前首次请求仍在 HTTP 请求内同步执行。
- [ ] 为长记录增加进度、取消和超时状态，并限制并发分析占用。
- [ ] 建立分析缓存的磁盘清理和容量策略。

## P0：部署与安全

- [ ] 将生产/测试进程纳入可控的 systemd 或容器编排生命周期，支持变更后的明确重启和回滚。
- [ ] 增加认证、请求体/参数大小限制、日志脱敏和数据目录最小权限检查。
- [ ] 将部署健康检查扩展到关键分析接口和版本/协议字段。

## P0：集成测试与持续集成

- [x] 在 CI 中运行单元测试、`py_compile`、官方真实样例分析和 PDF/CSV/API 契约测试。
- [x] 对三份样例记录固定关键摘要，检测 Cedalion 升级或参数变化造成的科学回归。

## P1：正式分析能力

- [ ] 将 Wavelet 从连续信号实验比较扩展为可配置、可审计的正式任务处理流程。
- [ ] 在网页中配置辅助生理信号名称、最大插值缺口和时间对齐策略（当前主要依赖环境变量）。
- [ ] 通过独立 sidecar 编辑/校正任务事件名称和开始/结束时间，始终不修改源 SNIRF。
- [ ] 增加探头/通道拓扑图、脑区映射和 ROI 汇总，并明确坐标缺失时的降级状态。
- [ ] 支持 BIDS derivatives 导出、批量记录/受试者分析、组水平模型和组水平报告。

## P2：实时与设备服务（独立轨道）

- [ ] 实时采集、开始/停止记录、设备控制和断线恢复。
- [ ] 将设备服务与离线 Cedalion 分析服务隔离，定义数据协议、权限和实时质量指标。

## 当前已知限制

- 分析设置页支持常用科学参数及重采样模式/目标率/最大缺口并保存到服务器；“恢复服务器默认值”会重新采用启动时的环境变量配置。
- 设置修改会使后续请求重新分析，但不会修改原始 SNIRF 文件。
- 单条记录 PDF 报告已存在，但组水平报告、实时采集和正式事件编辑仍未实现。

## 2026-08-17 真实回归与 CI 批次

- 本地完整回归 `41/41` 通过，包含三份真实 SNIRF、输入失败路径、进程内 API/清单契约、PDF/CSV、科学护栏和重采样测试。
- `test_real_snirf.py` 固定三份样例的任务就绪度、边界事件审计、通道摘要、时间单位和重采样审计，并比较分析前后的源文件 SHA-256。
- 新增 `.github/workflows/fnirs-dashboard.yml`：固定 Python 3.11/Cedalion 26.5.1，运行编译、完整测试和带 SHA-256 校验的官方 SNIRF smoke test。

## 2026-08-17 本批次记录

- 科学护栏：本地 `py_compile` 通过，单元测试 21/21 通过；三份真实 SNIRF 分别验证为 `ready`、`exploratory`（含 1 个边界事件排除）和 `unavailable`。
- 导出/API：真实探索性记录的任务 CSV、GLM CSV、PDF 均生成成功；临时前台进程验证 `/api/health`、`/api/recording`、`/api/task-glm`、GLM CSV 和 PDF 接口均为 HTTP 200，并返回 manifest `1.3` / protocol `v7` 和推断就绪度字段。
- 部署：源文件已部署到测试服务器 `/srv/testdata/projects/fnirs-dashboard`；远端 stage 和最终目录测试均为 21/21，回滚备份为 `.deploy-backup-20260817105756`。当前 SSH 隔离环境不能重启持久服务，运行中的服务待管理员重启后才会加载本批次代码。

## 2026-08-17 Wavelet 延迟计算批次

- Wavelet 不再在 `_load_analysis_cached()` 中执行；`/api/signal?kind=conc_wavelet_filtered` 首次请求时才计算，并由 `RLock` 保护单对象缓存。
- 新增 `test_lazy_wavelet.py`：真实样例加载不触发 Wavelet，重复信号请求只计算一次；真实记录请求验证成功。
- 本地完整回归 `44/44` 通过（官方样例 smoke test 因未设置专用环境变量跳过）；`py_compile` 与 `git diff --check` 通过。

## 2026-08-17 GLM 延迟计算批次

- `load_analysis(..., include_glm=False)` 和 Dashboard 非 GLM 接口共享同一个基础缓存对象；每个对象用 `RLock` 保证重复或并发请求只拟合一次，失败/不可用结果也缓存为终态。
- `/api/recording` 返回结构化 `pending` 状态；任务页在第一次请求 `/api/task-glm` 后更新状态和结果，`/api/report-pdf`、分析清单及 GLM CSV 会显式确保拟合。
- 新增 `test_lazy_glm.py` 覆盖跳过拟合、并发单次拟合、失败终态和自动建模通道选择；本地完整回归 `48/48` 通过（官方样例 smoke test 未启用而跳过）。
- 部署：源码已同步到 `/srv/testdata/projects/fnirs-dashboard`，远端暂存/最终目录编译和延迟测试通过；自动建模通道修复后的最新回滚备份为 `.deploy-backup-20260817095814`。临时应用验证 `/api/health`、`/api/recording`、`/api/quality`、Wavelet 信号、GLM CSV 和 `/api/task-glm?channel=auto` 均通过；持久服务仍待管理员重启后加载新代码。
